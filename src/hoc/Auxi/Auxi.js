//this file is only for , return children, so we did not used fragment so used auxi as HOC
//so now dont use extra div as Root element, use Auxi

const auxi = (props) => props.children;

export default auxi;
